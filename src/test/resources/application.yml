spring:
  datasource:
      driver-class-name: org.h2.Driver
      url: jdbc:h2:mem:alura;
      username: sa
      password: 
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
      properties:      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        
  flyway:
    enabled: true
    locations: classpath:db/migration